import { styled } from "styled-components";

export const Container = styled.div`
  position: fixed;
  z-index: 2;
  width: max-content;
  bottom: 48px;
  left: 50%;
  transform: translateX(-50%);
`;
